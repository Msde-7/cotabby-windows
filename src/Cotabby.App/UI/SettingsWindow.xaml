<Window x:Class="Cotabby.App.UI.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cotabby Settings"
        Width="540"
        Height="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize"
        Background="#FF1E1E1E"
        Foreground="#FFE0E0E0">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFE0E0E0" />
            <Setter Property="FontFamily" Value="Segoe UI" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="12,4" />
        </Style>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontSize="20" FontWeight="SemiBold" Text="Cotabby" />
        <TextBlock Grid.Row="1" Margin="0,4,0,12" FontSize="12" Opacity="0.7"
                   Text="Local AI autocomplete. Pick a model below and click Use." />

        <ListBox x:Name="ModelList"
                 Grid.Row="2"
                 Background="#FF252525"
                 Foreground="#FFE0E0E0"
                 BorderBrush="#FF3A3A3A"
                 BorderThickness="1"
                 SelectionMode="Single">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="6,8">
                        <TextBlock Text="{Binding DisplayName}" FontSize="14" />
                        <TextBlock Text="{Binding SizeDisplay}" FontSize="11" Opacity="0.6" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <ProgressBar x:Name="DownloadProgress"
                     Grid.Row="3"
                     Margin="0,12,0,4"
                     Height="6"
                     Minimum="0"
                     Maximum="1"
                     Value="0"
                     Visibility="Collapsed" />
        <TextBlock x:Name="StatusText"
                   Grid.Row="4"
                   Margin="0,4,0,12"
                   FontSize="12"
                   Opacity="0.8" />

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CancelButton" Content="Close" Margin="0,0,8,0" Click="OnClose" />
            <Button x:Name="UseButton" Content="Use selected" Click="OnUse" />
        </StackPanel>
    </Grid>
</Window>
